table "main"."table100" {
  primaryKey = {
    columns = [
      "id"
    ]
    name = "table100_pkey"
    objectType = primaryKey
  }
  owner = postgres
  columns = (
    column "id" {
      type = integer
      null = NOT NULL
    }
    column "name" {
      type = character varying(100)
    }
  )
}

